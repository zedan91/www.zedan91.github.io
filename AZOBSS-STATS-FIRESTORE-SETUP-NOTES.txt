AZOBSS Software Stats Fix

What changed:
- Removed duplicate/old stats renderer blocks in Software-Tools/index.html.
- Kept one final Firestore stats script only.
- Downloads and ratings write only to settings/softwareStats.items.
- Removed backend/data/software-stats.json if present.

Firebase required:
1) Authentication > Sign-in method > Anonymous = Enable.
2) Firestore document exists:
   settings/softwareStats
   { "items": {} }
3) Firestore Rules must allow settings/softwareStats update with affectedKeys ["items"].
   Use/merge FIREBASE-RULES-SOFTWARESTATS-BLOCK.txt.

If still fails, open browser DevTools > Console and check for:
- permission-denied = rules/auth issue.
- auth/operation-not-allowed = Anonymous Auth not enabled.
