AZOBSS PRIORITY PATCH 236P1

Priority 1 - Critical / role safety
- Added /assets/js/azobss-priority-role-guard.js.
- Admin Dashboard button now shows only after verified admin auth/profile.
- Staff Dashboard button now shows only after verified staff/semiAdmin auth/profile.
- Stale localStorage admin/staff role caches are cleared when current auth user is not verified for that role.
- Admin page top access guard now accepts both admin emails and no longer trusts unrelated zedan91 profile reads for other users.

Priority 2 - Admin Dashboard function
- Fixed Activity tab route: it now calls loadAdminActivity() instead of the undefined/old loadActivity().

Priority 3 - Registered User Records
- Search now uses explicit username/email/phone/uid/role fields.
- Load increased to 500 records with ordered query + fallback.
- Rows now show Online/Offline, PA/BM Yes/No, role badge, Edit and Delete buttons clearly.

No Firebase Rules update required for this patch.
