AZOBSS Member / Invite Code Register Fix

Fix applied:
- New signup no longer overwrites the entered member code with buildInviteCode(username).
- If user enters ZX6186 during signup, Firestore saves:
  inviteCode = ZX6186
  inviteCodeUsed = ZX6186
  invitedByCode = ZX6186
  memberCode = ZX6186
  paMemberCode = ZX6186
  paBmAccess = true
  paBmAllowed = true
  allowPABM = true
  paAccess = yes
- If member code is empty/wrong, PA/BM remains off.

Files updated:
- assets/js/azobss-global-auth.js
- assets/js/azobss-firebase-live-likes-sync.js

Existing users registered before this fix must be updated manually in Firestore once.
