AZOBSS PATCH 409 - Lucky Draw Minimum 100 Participants Note

Request:
- On Lucky Draw, under the running countdown/time, add a note:
  if participants do not reach 100 people, the roll will not run and the time will reset to next month.

Fix:
- Added a highlighted note below the Lucky Draw countdown panel.
- Malay text:
  Jika peserta tidak mencapai 100 orang, roll tidak akan berjalan. Countdown akan reset dan dibawa ke bulan seterusnya.
- English language toggle text:
  If participants do not reach 100 people, the roll will not run. The countdown will reset and move to next month.
- Added matching CSS for a small warning/info card.

Not changed:
- Lucky Draw backend remains the confirmed correct API backend from patch 408:
  https://azobss-lucky-draw-api.onrender.com
- No change to participant/draw logic. This patch only adds the visible notice text.
- Subscription/commission fixes remain.

Firebase Rules:
- No update required.

Render ENV:
- No new ENV required.
