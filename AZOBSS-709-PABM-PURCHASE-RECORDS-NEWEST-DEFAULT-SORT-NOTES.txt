AZOBSS Patch 709 - PA/BM Purchase Records Newest Default Sort

Changes:
- Purchase Records Users now defaults to "Kemas kini terbaharu".
- The user with the latest paid/verified purchase appears first when the PA/BM page opens.
- Existing sort choices remain available.
- Both purchase UI modules apply the default only once on initial page load, so changing the dropdown manually still works normally.
- PA/BM JavaScript cache versions were bumped so browsers load the fix immediately.

Baseline: Patch 708.
