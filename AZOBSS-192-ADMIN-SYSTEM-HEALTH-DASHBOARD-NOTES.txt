AZOBSS Patch 192 - Admin System Health Dashboard

Baseline: (191)-AZOBSS-ADMIN-BACKUP-EXPORT-REPORTS-FIX_20260617.zip

Changes:
1. Added protected backend endpoint:
   GET /api/admin/system-health

2. Endpoint requires:
   - Firebase admin ID token, or
   - AZOBSS_ADMIN_API_SECRET / AZOBSS_COMMISSION_API_SECRET

3. Added Admin Dashboard tab:
   🩺 System Health

4. Health check shows:
   - Required/recommended Render ENV presence without exposing secrets
   - Firebase Admin configured status
   - Firestore collection readability samples
   - Local JSON fallback file existence and sizes
   - Backend runtime info (Node version, uptime, memory, CORS, rate limit, receipt strict mode, ToyyibPay callback verify mode)

5. Added rate limit:
   admin-system-health

6. Health check requests are recorded in adminAuditLogs:
   admin_system_health_check

No Firebase Rules update required.
No required Render ENV added.
