AZOBSS ADMIN ACCESS GUARD

Added:
- /admin/ page is hidden by default.
- Guest sees Admin Only message.
- Normal user sees Access denied.
- Only admin can view dashboard.
- Admin is confirmed by Firebase Auth email or users/{username}.role == admin / zedan91.

No Firebase Rules update required for this front-end guard.
For stronger security, Firestore Rules should still protect admin collections.
