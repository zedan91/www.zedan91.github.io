# AZOBSS Patch 879 — Home Stickybar Exact Sync + Larger Wrapping Sound Filters

- Sound Effects now uses the same Home stickybar markup, exact-sync CSS asset, More navigation behaviour, and global auth/user controls used by the Home-aligned service page.
- The old marketplace search row remains hidden, matching the current one-row Home stickybar.
- Sound category/filter buttons are larger and easier to read/tap.
- Filters now wrap onto additional rows when horizontal space is insufficient instead of shrinking or clipping off-screen.
- Search becomes a full row on mobile while category buttons continue below it.
- Sound Effects features from 878 remain unchanged, including zoom sync, favorites, duration countdown, multi-download, deep links, infinite scroll and admin auto-update.
