AZOBSS PATCH 424 - Payment Logs Delete Backend Entrypoints Fix

Issue:
- Payment Logs delete still showed:
  "Payment Logs delete endpoint belum aktif / belum deploy di Render backend. Response: File Not Found"
- This proves the live backend did not have:
  POST /api/admin/payment-logs/delete

Likely reason:
- Patch 420/423 added the route mainly in deploy-server.js.
- If Render is running backend/server.js as the entrypoint, the endpoint returns File Not Found.

Fix:
- Added the same delete endpoint to backend/server.js:
  POST /api/admin/payment-logs/delete
- Endpoint uses existing requireAdmin middleware.
- Can delete:
  - purchaseLogs Firestore docs
  - premiumOrders Firestore docs
  - local premium-orders.json backup entries for matching premiumOrders
- Response includes:
  patch: 424
  runningFile: backend/server.js
- deploy-server.js marker bumped to patch 424 too.

Expected result:
- Whether Render runs deploy-server.js or backend/server.js, the delete endpoint should exist after Render redeploy.
- Clear Pending / Delete Selected should no longer return File Not Found.

Important:
- Render backend must be redeployed after uploading this ZIP.
- GitHub Pages deploy alone cannot create backend endpoints.

Firebase Rules:
- No update required because backend uses Firebase Admin.

Render ENV:
- No new ENV required.
