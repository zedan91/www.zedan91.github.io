AZOBSS SOFTWARE ROLE/CARDS FIX

Audit findings:
1. Main Software Tools JS had syntax error:
   alert('<span class='admin-only'>Import JSON</span> first.');

   This breaks the whole script, so:
   - admin toolbar does not open
   - cards do not render
   - filters still show because they are static HTML

2. Fixed alert quote to:
   alert('Import JSON first.');

3. Made getCurrentUserObject available for refreshAdminState role detection.

4. Admin:
   - Add/Import/Export/Apply/Refresh visible again.

5. Semi-admin/staff:
   - Add Software visible.
   - import/export/apply/refresh hidden.

Syntax errors after fix:
0

Verify:
{'syntax_errors': 0, 'has_alert_bad': False, 'grid_empty': True, 'add_button_exists': True, 'refreshAdminState': True}
