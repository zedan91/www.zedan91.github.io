AZOBSS CART FIRESTORE PULL RESTORE FIX

Diagnosis:
- Firestore save is working because userCarts/{uid} contains items.
- Remaining bug was restore/load into cart UI after login or after cache clear.

Fix:
- Cart UI now reads window.__azobssCurrentCartKey.
- Login sets user key before Firestore pull.
- Existing Firestore userCarts/{uid} is treated as source of truth.
- Cloud items are written into the correct user localStorage key.
- Cart UI refreshes immediately after Firestore pull.

Firebase Rules:
- No new rules needed if userCarts/{uid} rule was already published.
