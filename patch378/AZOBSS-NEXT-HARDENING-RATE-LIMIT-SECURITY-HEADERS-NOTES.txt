AZOBSS Next Hardening Patch 181

Baseline: (180)-AZOBSS-ADMIN-COMMISSION-FALLBACK-API-PROTECT-FIX_20260617.zip

Changes:
1. Added in-memory rate limits for sensitive backend endpoints:
   - POST /api/toyyib/create-pa-bm-bill
   - POST /api/toyyib/create-bill and /api/create-payment
   - GET /api/commission/status?records=1
   - POST /api/commission/retry-order
   - GET/POST /api/premium/download/:token
   - GET /api/premium/receipt/:orderId

2. Added common security headers to backend responses:
   - X-Content-Type-Options: nosniff
   - X-Frame-Options: SAMEORIGIN
   - Referrer-Policy: strict-origin-when-cross-origin
   - Permissions-Policy: camera=(), microphone=(), geolocation=()
   - Access-Control-Max-Age: 600

3. Added optional CORS hardening ENV:
   - AZOBSS_CORS_ORIGIN=https://www.azobss.com
   Default remains * to avoid breaking current deployment.

4. Added optional emergency disable for rate limiting:
   - AZOBSS_DISABLE_RATE_LIMIT=1
   Keep unset in production.

5. Applied the same security header helper to direct file/download response paths.

No Firebase Rules update required.
