# AZOBSS Patch 801

Adds a **🛠️ Service Bookings** shortcut beside **＋ Create Invoice / Receipt** in Admin → Sales & Receipts.

The shortcut uses the existing sidebar navigation so Service Bookings opens normally and its sidebar item is highlighted.
