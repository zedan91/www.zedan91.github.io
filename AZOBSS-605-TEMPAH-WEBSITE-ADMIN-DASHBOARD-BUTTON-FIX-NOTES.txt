AZOBSS PATCH 605

Fix:
- Restores the Admin Dashboard button on /Tempah-Website/ for admin users.
- The button markup and CSS already existed, but the page did not run the role-detection script that adds the admin body class and reveals the button.
- Adds the same role-aware dashboard button handling used on the other main pages.
- Staff users continue to see only Staff Dashboard; normal members see neither dashboard button.
- Rechecks after authentication/UI initialization, focus, storage changes and AZOBSS auth-ready events.

Deployment:
- Frontend only.
- Update GitHub Pages.
- No Render backend redeploy required.
- No Firebase Rules update required.
