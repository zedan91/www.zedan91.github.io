AZOBSS Radio Navbar Username Button Fix (Patch 358)

Purpose:
- Move the AZOBSS Radio button from the floating bottom/right position into the top navbar.
- Place the Radio button directly before the username/account area on desktop and phone.

Changes:
- assets/js/azobss-radio-player.js
  - Radio widget now inserts into #marketUserTools before #userMenu when available.
  - Floating mode remains only as fallback if navbar user tools are missing.
  - Radio panel opens as a right-aligned dropdown from the navbar button.
  - Keeps Minimize, Play, Stop, Volume, full Malaysia station list, page navigation restore, and Random Channel.
  - Radio still does not stop when minimized.
- HTML pages
  - Cache bust updated from v357 to v358.

No Firebase Rules update required.
No Render backend deploy required.
Deploy GitHub Pages only.
