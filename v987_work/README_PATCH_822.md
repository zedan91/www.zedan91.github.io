# AZOBSS Patch 822

This patch fixes the **RECEIPT NOT SENT** marker being clipped in **Admin → Sales & Receipts**.

The longer marker is now rendered as two controlled lines inside the Status cell, with a little more column space and safe inner margins so its full border and wording remain visible. All functions from patch 821 are retained.
