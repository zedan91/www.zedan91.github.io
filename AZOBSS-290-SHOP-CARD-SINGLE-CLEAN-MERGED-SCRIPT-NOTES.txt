AZOBSS Patch 290 - Shop Card Single Clean Merged Script Fix

Baseline used: (287)-AZOBSS-SHOP-CARD-FIRST-PAINT-CONTROLS-NO-BLINK-FIX_20260622.zip

Purpose:
- Clean up overlapping Software Tools scripts instead of adding another overlapping script.
- Keep Like / Share / Preview GIF / More / downloads / rating stable from first paint.
- Fix downloads and rating display by using one merged stats source.

Changes:
1. Software Tools
- Primary software module now supports Firestore softwareStats shape {items:{...}} and flat legacy cache.
- Software stats are prefetched from Firestore settings/softwareStats before dynamic card render.
- statsForItem / statsForCard now merge remote stats with product fallback instead of overwriting rating with zero.
- ratingVotes is preserved and rendered in first paint.
- Old duplicate AZOBSS SOFTWARE STATS FIRESTORE FINAL CLEAN script replaced with a single clean controller.
- New stats controller updates only existing text/star elements, not the entire footer innerHTML.
- Removed duplicate More-button scanner loop; More is now event-delegated only.

2. CAD Tools
- Added a small safety CSS marker to keep CAD controls visible.

Not touched:
- PA/BM paid/verified download flow.
- My Purchases.
- Cart storage/payment logic.
- Bell/message badge logic.
- Firebase Rules.
- Render ENV.

Firebase Rules: no update required.
Render ENV: no update required.
