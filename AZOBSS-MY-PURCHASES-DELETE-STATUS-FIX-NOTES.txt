AZOBSS PATCH 369 - MY PURCHASES DELETE + STATUS TEXT FIX

Changes:
1. My Purchases Pro now has a Delete button for each record.
2. Delete is soft-delete for the customer view only; admin/payment logs are not removed.
3. Pending payment records no longer show "Download locked".
4. Pending payment records now show: "Pending payment — download will appear after verified".
5. Paid records with used/expired Gmail download link now show clearer status such as "Downloaded 1/1" or "Download expired".
6. Added backend protected DELETE endpoint: /api/my-purchases/delete/:id
7. Added cache-bust for azobss-global-auth.js?v=369.

Deploy:
- GitHub Pages required.
- Render backend deploy required because deploy-server.js changed.
- Firebase Rules not required because backend uses Firebase Admin SDK.
