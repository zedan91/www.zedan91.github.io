# AZOBSS v1088 — Lot Processing Button Two-Line Layout

Baseline: v1087.

## Changes
- Enlarged the Lot Kadaster processing button slightly.
- `Sedang Diproses...` stays on the first row with the live spinner.
- `(Mengambil masa 1~2 Minit)` is now placed clearly on the second row below the main processing text.
- Compact/mobile sizing is adjusted so the two-line loading state remains readable without overflowing.
- Updated the `/PA-BM/` cache-buster for `azobss-lot-selection-map.js` to v1088.
- No changes to JUPEM processing, lot selection, pricing, geometry, cart, payment, or backend logic.

Package version: `1.0.1088`
