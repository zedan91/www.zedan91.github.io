AZOBSS Payment Popup Bank Default Fix

Changes made:
- Restored the old multi-option Complete Purchase popup.
- Bank Transfer is now the default active option.
- QR Payment, ToyyibPay, and Stripe tabs are visible again.
- Removed the simplified ToyyibPay-only create-bill handler that showed 'Invalid ToyyibPay response'.
- Open Payment Link now uses the product paymentLink field.
- I Have Paid button is restored.

Firebase Rules:
- No new Firebase Rules changes required for this UI/payment popup restore.

Render Environment:
- No new Render Environment variables required for this restore.
