AZOBSS PAYOUT RECEIPT / PROOF FIX (200)

Changes:
- Added protected payout receipt endpoint:
  GET /api/payout/receipt/:requestId
- Staff can view only their own payout receipt/statement.
- Admin can view any payout receipt/statement from Admin Dashboard.
- Receipt opens as printable HTML with Print / Save PDF button.
- Receipt includes request ID, status, amount, payout reference, method, account snapshot, linked commission IDs and timeline.
- Staff view masks sensitive payout account details; admin view can show full profile snapshot.
- Receipt access is protected by Firebase ID token or admin API secret.
- Receipt view is written to adminAuditLogs as payout_receipt_view.

No Firebase Rules update required.
No required Render ENV added.
