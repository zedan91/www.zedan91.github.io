# AZOBSS 1060 — Sales & Receipts Net Profit Clean Gold Card UI Fix

## Changes
- Uses v1059 as the baseline.
- Removes the small `AZOBSS • NET` stamp that overlapped the `NET PROFIT` title/value.
- Net Profit keeps the metallic gold highlight, but inherits the same KPI card height/padding/responsive sizing as the other Sales & Receipts cards.
- Centers the `NET PROFIT` label, amount, and explanatory note for a cleaner layout.
- Removes the oversized fixed Net Profit value font so responsive desktop/mobile KPI sizing remains consistent.
- Net profit calculations, invoices, payments, records, and backend logic are unchanged.

## Deployment
Website-only deployment.
