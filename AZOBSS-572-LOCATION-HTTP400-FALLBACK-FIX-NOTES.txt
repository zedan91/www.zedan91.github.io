AZOBSS 572 - Location Search HTTP 400 Fallback Fix

Fixed:
- Removed unsupported Photon language request `lang=ms`, which caused HTTP 400 on the public Photon service.
- Removed the Malay Accept-Language header so Photon can return its default/local place names safely.
- Added progressive provider fallback: state bounding box, state bias, minimal Malaysia filter, then bare Malaysia-focused search.
- Added safe parsing and clearer Malay error handling when the external provider is temporarily unavailable.
- Search remains restricted to Malaysian coordinates/results after provider response normalisation.
- azobss-lot-selection-map.js cache version bumped from v20 to v21.
- Package version bumped to 1.0.572.

Preserved from 571/570:
- One box for WGS84 coordinates or place names.
- Suggestions after 3 characters, keyboard navigation, zoom and red marker.
- All JUPEM map selection, estimate, token, cart, polling and verified download flows.

Deployment:
- Render backend redeploy required.
- GitHub Pages deploy required.
- No Firebase Rules change required.
