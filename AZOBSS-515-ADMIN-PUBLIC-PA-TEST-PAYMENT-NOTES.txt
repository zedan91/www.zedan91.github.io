AZOBSS PATCH 515 - ADMIN PUBLIC PA TEST PAYMENT

1. Adds a Test Payment (Admin) button on /Beli-Pelan-Akui/.
2. The button is visible only to the trusted admin account and remains disabled until PA existence is verified.
3. Admin test creates a paid RM50 public PA record without opening ToyyibPay or charging real money.
4. Returns working controlled-download and receipt URLs.
5. Test records are tagged isAdminTestPayment/testPayment, skip commission, and skip customer email.
6. Normal users still see only the real RM50 FPX payment flow.
7. Backend route added to both backend/server.js and deploy-server.js: POST /api/admin/test-public-pa-payment.
8. Render backend redeploy is required. No Firebase Rules change is required.
