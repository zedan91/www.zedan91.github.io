# Patch 817 — WhatsApp Shop Location Label

Built from patch **816**.

- Changes `Peta:` to `Lokasi Kedai:` in the service-booking WhatsApp message.
- Keeps the existing Google Maps directions URL from the customer-selected location to the AZOBSS shop.
- Synchronizes the official backend message and fast frontend fallback message.
- Preserves all functions from patch 816.
