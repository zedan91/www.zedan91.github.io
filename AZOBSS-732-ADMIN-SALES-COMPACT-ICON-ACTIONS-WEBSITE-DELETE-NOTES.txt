AZOBSS PATCH 732
Date: 2026-08-04
Baseline: Patch 731

Changes:
1. Removed the Unified Sales Ledger title and accounting explanation from the top card.
2. Reduced the top card to a compact bar containing Create, Refresh and Export CSV only.
3. Replaced the Receipt/Invoice dropdown and text action buttons with direct circular icons:
   - Download PDF
   - WhatsApp
   - Telegram
   - Print
   - Edit (manual records only)
   - Delete
4. Added Delete to website payment rows as well as manual rows.
5. Website delete uses the existing authenticated backend endpoint:
   POST /api/admin/payment-logs/delete
   It removes matching purchaseLogs/premiumOrders records and the Render local premium-orders backup when applicable.
6. Paid website record deletion shows an extra warning because it changes sales and profit totals.
7. No new Firebase rule, Cloudflare Worker or Environment Variable is required.
