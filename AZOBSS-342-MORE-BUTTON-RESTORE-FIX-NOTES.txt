AZOBSS PATCH 342 - SOFTWARE/CAD MORE BUTTON RESTORE FIX

Purpose:
- Restore the More button if it disappears after equal-card/bottom-CTA layout patches.
- Keep More stable without blinking/flicker.

Changes:
- Adds final CSS to force More visible as a sibling overlay above the CTA.
- Adds a small idempotent restore script that inserts More only when a long description needs it.
- Removes accidentally nested More buttons inside the description box.
- Applies to Software Tools, CAD Tools, and root fallback.

No Firebase Rules update required.
No Render backend deploy required.
Deploy GitHub Pages only.
