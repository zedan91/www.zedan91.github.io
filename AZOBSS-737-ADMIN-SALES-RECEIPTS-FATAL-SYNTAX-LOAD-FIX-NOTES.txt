AZOBSS Patch 737

Root cause fixed:
- The Sales & Receipts ES module had a missing closing parenthesis in bulkShareSelected().
- Because of that syntax error, the browser rejected the entire module before window.azSalesReceiptsLoad could be registered.
- This caused the dashboard message: Sales & Receipts module failed to load.

Fix:
- Corrected the Set(rows.map(...)) expression.
- Preserved all Patch 736 auto-load logic and Patch 735 PDF-link features.
- Cache version bumped to 737.

No Cloudflare Worker, Render ENV, backend, or Firebase Rules changes are required.
