AZOBSS PATCH 328 - Software/CAD Description More No-Blink Final Fix

Purpose:
- Fix remaining blinking/flicker on description and More button after 3-line clamp patches.

Root cause:
- Older patches 323/324/326 still had JavaScript normalizers, MutationObserver, delayed setTimeout style writes, and inline style updates.
- They competed with CSS and re-rendered card layout repeatedly.

Fix:
- Removed the active JS normalizer scripts from Software-Tools, CAD-Tools-&-Resources and root fallback.
- Final layout is CSS-only: 3-line clamp + ellipsis, More chip overlay, Download/Buy pinned near footer.
- Added a tiny one-time title tooltip script only; no MutationObserver and no repeated inline style writes.

Deploy:
- GitHub Pages only.
- Firebase Rules: no update required.
- Render backend: no deploy required.

Test:
- Hard refresh /Software-Tools/.
- Description should stay stable at 3 lines.
- More chip should not blink.
- Download Now / Buy Now should stay close to footer downloads/rating.
