AZOBSS QUOTA LIMIT PATCH

Applied quota-friendly limits:
- Cart: maximum 100 items.
- Support Messages:
  - Admin inbox loads latest 50 messages only.
  - User support view loads latest 20 messages only.
  - Chat badge scans latest 50 messages only.
- Notifications:
  - Bell popup loads latest 20 active notifications only.
  - Bell badge scans latest 50 active notifications only.
- Existing badge behavior preserved:
  - Bell badge disappears after opening notifications.
  - Chat badge disappears after opening support.
- No Firebase Rules change needed.

Recommended cleanup later:
- Delete/archive notifications older than 180 days.
- Archive support messages older than 180 days or delete after 365 days if no longer needed.
