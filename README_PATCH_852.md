# Patch 852 — More Menu Sound Effects

- Baseline: `(851)-AZOBSS-TECH-VAULT-WORKER-TXT-DELIVERY-FIX_20260809.zip`.
- Adds `Sound Effects` directly below `Mini Web Tools` inside the shared desktop/mobile `More` navigation menu.
- `Sound Effects` opens `https://www.myinstants.com/en/instant/chicken-on-tree-screaming-53890/` in a new tab with `noopener noreferrer`.
- Uses a waveform-style icon matching the existing More menu visual language.
- Existing `Bina Website`, `Food - Brownies`, `Repair PC`, and all other site functions are unchanged.
- Shared `azobss-more-nav.js` cache-buster updated from `v=780` to `v=852` on pages that load it.
