Patch 372 - Premium Never Expire Real Token Display Fix

- Fixes Software/CAD premium link expiry where Never expire could still become 24/48 hours due to Number(value) || 24 and product normalizer fallback.
- Preserves expiryHours = 0 from Software/CAD admin forms, product normalization, checkout payload, backend trusted product lookup and manual/admin test purchase flow.
- Hydrates paid pending orders from current trusted product settings before generating download token/email, so existing pending orders can recover from previous wrong 24h expiry if the product is now set to Never expire.
- Adds expiresNever flag to premium orders/tokens.
- Email text and download confirmation page now show Never expire instead of a date when product expiry is Never expire.
- Download token validation still enforces download limit; Never expire only disables date expiry.

Firebase Rules: no update required.
Render: deploy required because deploy-server.js changed.
GitHub Pages: deploy recommended because Software/CAD checkout JS changed.
