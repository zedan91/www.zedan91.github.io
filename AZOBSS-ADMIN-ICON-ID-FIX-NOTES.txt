AZOBSS ADMIN ICON ID FIX

Fixed:
- Admin icon visibility now checks u.id as well:
  usernameKey || username || id || name || displayName

Reason:
- Current saved user object has:
  id: "zedan91"
  role: "admin"

Also:
- Added small recheck trigger after login/focus.
- Did not edit login flow.
