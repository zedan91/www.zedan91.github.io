AZOBSS 242 - ADMIN FIREBASE RULES EMAIL SYNC FIX

Baseline source:
(241)-AZOBSS-ADMIN-USERS-COUNT-LIST-FIX_20260620.zip

Scope:
Admin Dashboard only - Step 4 after Admin Guard, Activity tab, and Users Count/List fixes.

What changed:
1. Firebase Rules admin email checks were aligned to accept both owner emails:
   - zedan91@azobss.local
   - zedan9107@gmail.com

2. The change was applied only to rule instruction files / Firestore rules text files that still used:
   request.auth.token.email == "zedan91@azobss.local"

3. The logic now uses:
   request.auth.token.email == "zedan91@azobss.local" || request.auth.token.email == "zedan9107@gmail.com"

Why:
Admin dashboard frontend already allows the real owner Gmail in the strict admin guard, but older Firestore Rules text files allowed only the local admin email. This mismatch can cause the admin UI to load while Firestore data appears empty or permission-denied.

Not touched:
- Cart / Like / Bell / Message badge
- My Purchases
- PA/BM paid/verified download flow logic
- Admin Activity UI
- Registered User Records UI

Important:
Firebase Console Rules must be updated manually using the latest desired rules text file from this ZIP. The recommended active rule reference is:
FIREBASE-RULES-AZOBSS-PA-BM-DOWNLOAD-LIMIT-5X-7D-COUNTER-FIX.txt

