AZOBSS PA/BM sticky access fix

- Sticky bar PA/BM now follows Firestore PA/BM allowed fields.
- If admin sets Yes - allow PA/BM tab, these aliases are saved:
  paBmAccess, paBmAllowed, allowPABM, allowPaBm, allowPaBmTab,
  paBmTabAllowed, showPaBmTab, canAccessPaBm, paAccess=yes.
- If user registered with ZX6186, PA/BM is treated as allowed.
- If admin sets No, sticky PA/BM is hidden after refresh/login.

Test:
1. Admin > Edit Registered User > choose Yes - allow PA/BM tab > Save.
2. User logout/login or hard refresh.
3. PA / BM button should appear in sticky bar.
