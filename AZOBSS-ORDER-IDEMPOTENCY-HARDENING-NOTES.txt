AZOBSS ORDER IDEMPOTENCY HARDENING - BUILD 184

Base:
(183)-AZOBSS-TOYYIBPAY-CALLBACK-VERIFY-LOG-SAFE-FIX_20260617.zip

What changed:
1. Added order-level finalization lock for ToyyibPay callback/verify flow.
2. Prevents duplicate email sending when callback and verify endpoint are triggered close together.
3. Prevents repeated commission finalization for already checked orders.
4. Prevents repeated PA/BM purchaseLogs paid sync after the first successful sync.
5. Adds email send attempt tracking and short in-progress guard.
6. Keeps old behavior for retry if email failed.

Firebase Rules:
No update required.

Render ENV:
No new mandatory ENV.
