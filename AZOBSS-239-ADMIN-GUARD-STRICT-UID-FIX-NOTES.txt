AZOBSS 239 - Admin Guard Strict UID Fix

Scope:
- Step 1 only: /admin/ access guard.
- Priority 2 skipped: Cart, Like, Bell, Message badge, My Purchases, PA/BM paid/verified download flow.

Fixed:
1. Removed unsafe admin access logic that could pass a non-admin user after reading users/zedan91.
2. /admin/ now checks Firebase Auth user first.
3. Owner emails allowed:
   - zedan9107@gmail.com
   - zedan91@azobss.local
4. Non-owner admin role is accepted only when the user profile belongs to the current Firebase Auth user via matching uid or matching email.
5. Stale admin role cache is cleared on denied access.
6. Old localStorage-based page guard no longer decides /admin/ access; the strict Firebase access guard is the source of truth.

Not changed:
- Admin Activity tab bug not changed in this step.
- Admin Overview count/list improvements not changed in this step.
- Firebase Rules not changed in this ZIP.
