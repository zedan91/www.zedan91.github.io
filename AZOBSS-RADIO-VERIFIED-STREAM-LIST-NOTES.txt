# AZOBSS RADIO VERIFIED STREAM LIST FIX (364)

Patch from (363).

Fixes:
- Removed risky/website-only Malaysia radio channels that often show "Stream tidak dijumpai".
- Kept a smaller verified/stable Malaysia channel list with top channels first.
- Added multiple search aliases per station so RadioBrowser lookup is less likely to fail.
- If a station is temporarily offline/not found, it is hidden locally for 12 hours instead of repeatedly showing the same broken stream.
- Cache-bust updated to /assets/js/azobss-radio-player.js?v=364 on all HTML pages.

No Firebase Rules update required.
No Render backend deploy required.
Deploy GitHub Pages only and hard refresh.
