AZOBSS STICKYBAR ICONS ACTION + BADGE FIX

Issue:
- Icon HTML matched Home, but dashboard pages may not have the same cart drawer/event bindings.
- Cart badge may not sync because dashboard page storage/event binding differs.
- Some icon href/data attributes can be wrong after copy.

Fixed:
- Likes always goes to /likes/
- Bell/Notifications always goes to /PA-BM/
- Chat always goes to https://alvo.chat/6nZ2
- Cart button gets data-az-shop-cart-toggle
- Cart badge gets data-az-shop-cart-badge
- Added lightweight binder for admin/staff:
  - opens existing cart drawer if found
  - fallback to /Software-Tools/
  - syncs badge from localStorage/sessionStorage cart keys
  - refreshes badge on storage/focus/interval

Patched:
['admin/index.html', 'staff/index.html']

Syntax errors:
0
