# Patch 857 — Sound Effects Unlimited Custom Embed Catalog

Built from patch **856**.

- Keeps the 81 curated built-in sounds and lazy/infinite scroll.
- Adds **+ Add sound** for individual official MyInstants sound URLs / embed URLs.
- Custom sounds use the provider's official embed endpoint; AZOBSS does not re-host the audio.
- Custom catalog is stored in browser localStorage and supports up to 500 items.
- Adds Custom category filter, delete, search, share, copy link and download fallback for custom cards.
- Adds Export JSON / Import JSON to move a custom catalog between devices.
- Cache-buster for shared More navigation updated to v857.
