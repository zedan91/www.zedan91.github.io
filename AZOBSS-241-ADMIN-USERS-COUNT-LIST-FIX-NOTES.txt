AZOBSS (241) Admin Users Count/List Fix

Baseline used:
(240)-AZOBSS-ADMIN-ACTIVITY-TAB-FIX_20260620.zip

Scope:
Admin Dashboard only.

Fixed:
1. Overview Total Users now uses Firestore aggregate count via getCountFromServer(collection(db,'users')) when available.
2. Fallback remains the previous limited query size if aggregate count fails.
3. Registered User Records load limit increased from 200 to 500.
4. Registered User Records page info now shows matched records plus loaded/total count.
5. Registered User Records search now checks focused user fields: doc id, username, email, phone, uid, role, member/invite/access code.

Not touched:
- Cart / Like / Bell / Message badge.
- My Purchases.
- PA/BM paid/verified download flow.
- Firebase Rules.
